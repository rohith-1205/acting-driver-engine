# Acting Driver Matching Engine

This service is the matching and routing engine for PublicRides' acting drivers.

## Setup Instructions

1. Install dependencies:
   ```bash
   npm install
   ```

2. Configure environment:
   ```bash
   cp .env.example .env
   ```
   Edit `.env` as required.

3. Run locally:
   ```bash
   npm run dev
   ```
